import Home from "../components/Home"
import Login from "../components/login"

export const routes = [
    {
        path: '/',
        component: Login
    },

    {
        path: '/home',
        component: Home
    }
]
